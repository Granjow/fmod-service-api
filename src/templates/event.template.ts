import { FmodEvent } from '../fmod-types';

// begin-template
class CLASSNAME extends FmodEvent {

    constructor() {
        super( 'EVENT_NAME', 'BANK_NAME' );
        // CONSTRUCTOR
        this.params.push( ...[
            // PARAM_LIST
        ] );
    }

    // PARAM_DEF
}

// end-template
